name: Publish Extensions

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # test:

  # build:
  #   strategy:
  #     matrix:
  #       node-version: [14.x]
  #       os:
  #         - macos-latest
  #         - ubuntu-latest
  #         - windows-latest

  #   runs-on: ${{ matrix.os }}

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v2

  #     - name: Use Node.js ${{ matrix.node-version }}
  #       uses: actions/setup-node@v2
  #       with:
  #         node-version: ${{ matrix.node-version }}

  #     - run: npm ci
  #     - run: npm run test
  #     - run: vsce publish --yarn --pat ${{ secrets.VSCE_TOKEN }} $(node -e \"console.log(require('./package.json').version)\")
